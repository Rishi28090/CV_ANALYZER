<!DOCTYPE html>
<html lang="en">
<head>
  <title>AI Resume Analyzer</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background-color: #f8f9fa; }
    .card { max-width: 500px; margin: auto; }
  </style>
</head>
<body>
  <div class="container py-5">
    <div class="text-center mb-4">
      <h1 class="display-5 fw-bold">AI Resume Analyzer</h1>
      <p class="text-muted">Upload your resume (PDF) and get smart feedback from Google Gemini AI</p>
    </div>

    <div class="card shadow p-4">
      <form action="/upload" method="POST" enctype="multipart/form-data" onsubmit="showSpinner()">
        <div class="mb-3">
          <label for="resume" class="form-label">Select Resume (PDF only)</label>
          <input type="file" class="form-control" id="resume" name="resume" accept=".pdf" required>
        </div>
        <div class="d-grid">
          <button class="btn btn-primary" type="submit">Analyze Now</button>
        </div>
      </form>

      <div id="spinner" class="text-center mt-3 d-none">
        <div class="spinner-border text-primary" role="status"></div>
        <p class="mt-2">Analyzing resume...</p>
      </div>
    </div>
  </div>

  <script>
    function showSpinner() {
      document.getElementById("spinner").classList.remove("d-none");
    }
  </script>
</body>
</html>
